{% extends 'JmSaleBundle:Default:admin-base.html.twig' %}
{% block body %}
<h1>Fecha list</h1>

<table class="records_list table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Orden</th>
            <th>Nombre</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_fecha_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.orden }}</td>
            <td>{{ entity.torneo.nombre }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_fecha_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_fecha_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_fecha_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}