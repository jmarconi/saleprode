{% extends 'JmSaleBundle:Default:admin-base.html.twig' %}
{% block body %}
<h1>Equipo creation</h1>

<form action="{{ path('admin_equipo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_equipo') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}