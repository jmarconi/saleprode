{% extends 'JmSaleBundle:Default:admin-base.html.twig' %}
{% block body %}
<h1>Apuesta creation</h1>

<form action="{{ path('admin_apuesta_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_apuesta') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}